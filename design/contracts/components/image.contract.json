{
  "$schema": "../schemas/component-contract.schema.json",
  "id": "image",
  "version": "0.1.0",
  "name": "Image",
  "category": "data-display",
  "appStatus": "implemented",
  "intent": "画像表示 primitive。aspectRatio / radius / contentFit / onError fallback を持つ。Phase1 は RN コア Image、expo-image は Phase2。",
  "docPath": "components/image.md",
  "appFirst": true,
  "webStatus": "pending",
  "anatomy": ["image", "fallback?"],
  "variants": {
    "default": {
      "description": "標準画像（角丸つき）",
      "tokenRefs": {
        "radius": "radius.md"
      },
      "tailwind": ""
    }
  },
  "sizes": {},
  "states": ["default", "error"],
  "a11y": {
    "role": "image",
    "required": ["情報を持つ画像は accessibilityLabel 必須", "装飾画像は読み上げ対象から外す"],
    "keyboard": [],
    "focusRing": "N/A"
  },
  "rules": []
}
